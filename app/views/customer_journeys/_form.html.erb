<%= form_for(customer_journey) do |f| %>
  <% if customer_journey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_journey.errors.count, "error") %> prohibited this customer_journey from being saved:</h2>

      <ul>
      <% customer_journey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
